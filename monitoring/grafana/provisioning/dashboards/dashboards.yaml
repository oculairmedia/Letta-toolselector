apiVersion: 1

providers:
  - name: 'Tool Selector Dashboards'
    orgId: 1
    folder: 'Tool Selector'
    folderUid: 'tool-selector'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
